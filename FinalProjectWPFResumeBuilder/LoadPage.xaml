<Window x:Class="FinalProjectWPFResumeBuilder.LoadPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FinalProjectWPFResumeBuilder"
        mc:Ignorable="d"
        Title="LoadPage" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="DataGrid">
            <Setter Property="Height" Value="200"/>
            <Setter Property="Width" Value="575"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="AutoGenerateColumns" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
        </Style>
    </Window.Resources>
    <DockPanel>
        <Label DockPanel.Dock="Top" Content="Load Page" />
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Center" >
            <Button x:Name="GoBack" Click="GoBack_Click" Content="Go Back"/>

        </StackPanel>

        <DataGrid x:Name="CategoryDataGrid" SelectionChanged="CategoryDataGrid_SelectionChanged"/>


    </DockPanel>
</Window>
